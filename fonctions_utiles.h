#include <stdio.h>
#include <stdlib.h>

typedef enum etat etat;
enum etat 
{
	fonctionne = 1;
	en_panne = 0;
};

int MachineEnPanne (pid numero_machine)
{
	
}

void send(char message[25],pid n_processus)
{
	printf("%s/n", message);
}
	
void send (char message[50]
